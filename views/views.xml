<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="tms.list">
      <field name="name">TMS list</field>
      <field name="model">tms.tms</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="tms.action_window">
      <field name="name">TMS window</field>
      <field name="res_model">tms.tms</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="tms.action_server">
      <field name="name">TMS server</field>
      <field name="model_id" ref="model_tms_tms"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="TMS" id="tms.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="tms.menu_1" parent="tms.menu_root"/>
    <menuitem name="Menu 2" id="tms.menu_2" parent="tms.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="tms.menu_1_list" parent="tms.menu_1"
              action="tms.action_window"/>
    <menuitem name="Server to list" id="tms" parent="tms.menu_2"
              action="tms.action_server"/>
    -->
  </data>
</openerp>